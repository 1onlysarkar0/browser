services:
  - type: web
    name: browser-automation
    plan: starter # or free / standard depending on your account
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
    docker:
      webService:
        dockerfilePath: ./Dockerfile
