services:
  - type: web
    name: browser-automation
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
    docker:
      webService:
        dockerfilePath: ./Dockerfile
